<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav"
    app:startDestination="@id/currentTempFragment">
    <fragment
        android:id="@+id/currentTempFragment"
        android:name="com.tallner.apiintegration.CurrentTempFragment"
        android:label="Current temperature"
        tools:layout="@layout/fragment_current_temp" >
    </fragment>

    <fragment
        android:id="@+id/forecastFragment"
        android:name="com.tallner.apiintegration.ForecastFragment"
        android:label="Forecast"
        tools:layout="@layout/fragment_forecast" >
    </fragment>

    <fragment
        android:id="@+id/thirdFragment"
        android:name="com.tallner.apiintegration.ThirdFragment"
        android:label="Profile"
        tools:layout="@layout/fragment_third">
        </fragment>
</navigation>